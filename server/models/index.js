//Import the User and Locker models from the models directory
const User = require('../models/User');
const Locker = require('../models/Locker');

//Export the User and Locker models as an object
module.exports = { User, Locker };